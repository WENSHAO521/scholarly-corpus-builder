"""Style Analytics Engine (PART XVII-XXII). Rule-based, measured
descriptive analytics over already-extracted text — not an ML pipeline.
Every metric here is a sampled heuristic proxy, not an exact linguistic
measurement (see references/style-feature-schema.md); modules that
classify rather than count (claims.py, rhetorical.py, rhythm.py) must be
reported to callers as "inferred", never as objective fact.
"""
