"""Source Adapter Layer (PART III). Each adapter normalizes into
scb.records.CanonicalRecord and declares which capabilities it actually
supports — no adapter pretends to support an operation its upstream API
doesn't offer.
"""
